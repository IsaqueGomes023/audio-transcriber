# AudioTranscriber — Dependências
# Instale com: pip install -r requirements.txt
# (O instalar.bat faz isso automaticamente no Windows)

# ─── Interface ───────────────────────────────────────────────────────────────
streamlit>=1.35.0

# ─── API Groq (transcrição Whisper + sumarização Llama 3) ────────────────────
groq>=0.9.0

# ─── Exportação de documentos ────────────────────────────────────────────────
python-docx>=1.1.0

# ─── Variáveis de ambiente (.env) ────────────────────────────────────────────
python-dotenv>=1.0.0

# Nota: para M4A/MP4 e arquivos > 25 MB, o FFmpeg deve estar instalado no sistema.
# No Windows: winget install ffmpeg
# No Streamlit Cloud: declarado em packages.txt (incluído no repositório)
